# 3.0.0

- Các công thức đã xuất giờ đây có thể được tải như tiện ích mở rộng bằng cách đặt chúng trong thư mục `plugin_data/Mineflow/addons`.
  - Các công thức được tải bằng tiện ích mở rộng không hiển thị trong danh sách công thức.
  - Các lệnh sau đã được thêm vào:
    - `/mineflow addon list`: hiển thị danh sách các tiện ích mở rộng
    - `/mineflow addon reload`: tải lại các tiện ích mở rộng trong thư mục `plugin_data/Mineflow/addons`
    - `/mineflow addon load <name>`: tải tiện ích mở rộng `plugin_data/Mineflow/addons/<name>.json`
    - `/mineflow addon unload <name>`: hủy tải tiện ích mở rộng có tên `<name>`
  - Các hành động tùy chỉnh giờ đây có thể được thêm vào bằng cách sử dụng tiện ích mở rộng.
- Các công thức giờ đây có thể được tạm thời vô hiệu hóa.
- Đối số công thức giờ đây có thể chỉ định một loại biến.
- Việc bật/tắt kích hoạt sự kiện giờ đây có hiệu lực ngay lập tức.
- Đã thêm các biến mới
  - `_.args`: danh sách các đối số công thức
- Đã thêm thuộc tính biến mới
  - Biến số
    - floor
    - ceil
    - round
    - abs
- Đã thêm phương thức biến mới
  - Biến số
    - floor()
    - ceil()
    - round(precision)
    - abs()
    - pad(length)
- Đã thêm các hành động mới
  - Đặt dữ liệu người chơi
    - Lưu trữ dữ liệu cá nhân của người chơi trong biến người chơi. Để lấy dữ liệu từ biến người chơi, bạn có thể sử dụng biến `<tên biến người chơi>.data.<tên dữ liệu>`. Ví dụ) `target.data.money`
  - Đặt giá trị mặc định cho dữ liệu người chơi
- Các hành động trong danh mục `Economy` đã được chuyển sang plugin [MineflowMoreActions](https://github.com/aieuo/MineflowMoreActions).

## API

### Main

- Các phương thức sau đã được di chuyển / đổi tên
  - `Main::getConfig()` -> `Mineflow::getConfig()`
  - `Main::getPlayerSettings()` -> `Mineflow::getPlayerSettings()`
  - `Main::getRecipeManager()` -> `Mineflow::getRecipeManager()`
  - `Main::getCommandManager()` -> `Mineflow::getCommandManager()`
  - `Main::getFormManager()` -> `Mineflow::getFormManager()`
  - `Main::getEventManager()` -> `Mineflow::getEventManager()`
  - `Main::getVariableHelper()` -> `Mineflow::getVariableHelper()`
  - `Main::getPluginVersion()` -> `Mineflow::getPluginVersion()`
  - `Main::getTimeTriggerTimeZone()` -> `Mineflow::getTimeTriggerTimeZone()`
  - `Main::isEnabledRecipeErrorInConsole()` -> `Mineflow::isEnabledRecipeErrorInConsole()`
  - `Main::setEnabledRecipeErrorInConsole()` -> `Mineflow::setEnabledRecipeErrorInConsole()`
- Các phương thức sau đã được thêm vào
  - `Mineflow::getAddonManager()`

### Variable

- Loại nhận dạng biến đã được thay đổi từ int sang string
- `Variable->callMethod()` is now final.
  - Để thêm phương thức biến, hãy sử dụng `Variable::registerMethod()` thay thế.
- Các lớp sau đã được di chuyển / đổi tên
  - `BoolVariable` -> `BooleanVariable`
  - `(.+)ObjectVariable` -> `$1Variable`
- Các phương thức sau đã được di chuyển / đổi tên
  - `Variable->getValueFromIndex()` -> `Variable->getProperty()`
    - Để thêm thuộc tính biến, hãy sử dụng `Variable::registerProperty()` thay thế.
  - `ObjectVariable->getValueType()` -> `ObjectVariable->getValueClass()`
  - `ObjectVariable::getValuesDummy()` -> `Variable::getPropertyTypes()`
- Chữ ký của các phương thức sau đã được thay đổi
  - `Variable->__construct($value)` -> `Variable->__construct()`
  - `Variable->add($variable): Variable` -> `Variable->add(Variable $variable): Variable`
  - `Variable->sub($variable): Variable` -> `Variable->sub(Variable $variable): Variable`
  - `Variable->mul($variable): Variable` -> `Variable->mul(Variable $variable): Variable`
  - `Variable->div($variable): Variable` -> `Variable->div(Variable $variable): Variable`
  - `ObjectVariable->__construct(object $value, ?string $str = null)` -> `ObjectVariable->__construct()`
  - `DummyVariable->__construct(string $valueType = "", string $description = "")` -> `DummyVariable->__construct(class-string<Variable> $valueClass = "", string $description = "")`
- Các phương thức sau đã được thêm vào
  - `Variable->getTypeName()`
  - `Variable::registerProperty()`
  - `Variable::getPropertyObject()`
  - `Variable::getProperties()`
  - `Variable::getPropertyTypes()`
  - `Variable::registerMethod()`
  - `Variable::getMethod(string $name)`
  - `Variable::getMethods()`
  - `Variable::getMethodTypes()`
  - `VariableHelper->getAll()`
- Các phương thức sau đã được xóa
  - `Variable->getType()`
    - Sử dụng `Variable->getTypeName()` thay thế.
  - `ObjectVariable->getShowString()`
    - Sử dụng `Variable::__toString()` thay thế.
  - `Variable->fromArray()`
    - Sử dụng `VariableDeserializer->deserialize()`
  - `Variable->map()`

### Recipe

- `Recipe->loadSaveData` now loads trigger, argument, and return values.
- Chữ ký của các phương thức sau đã được thay đổi
  - `Recipe->executeAllTargets(?Entity $player = null, array $variables = [], ?Event $event = null, array $args = [], ?FlowItemExecutor $callbackExecutor = null): ?bool` -> `Recipe->executeAllTargets(?Entity $player = null, array $variables = [], ?Event $event = null, array $args = [], ?FlowItemExecutor $from = null, ?callable $callback = null): ?bool`
  - `Recipe->execute(?Entity $target, ?Event $event = null, array $variables = [], array $args = [], ?FlowItemExecutor $callbackExecutor = null): bool` -> `Recipe->execute(?Entity $target, ?Event $event = null, array $variables = [], array $arguments = [], ?FlowItemExecutor $from = null, ?callable $callback = null): bool`
- Các phương thức sau đã được thêm vào
  - `Recipe->setEnabled()`
  - `Recipe->isEnabled()`
  - `Recipe->addArgument()`
  - `Recipe->removeArgument()`
  - `Recipe->getAddonDependencies()`
  - `Recipe->getPluginDependencies()`

### FlowItem

- `FlowItem->id` and `FlowItem->category` property is now private.
- Quyền FlowItem giờ đây sử dụng tên quyền thay vì cấp độ quyền.
- `FlowItem->getName()`, `FlowItem->getDescription()`, and `FlowItem->getDetail()` is now abstract.
- Các hành động giờ đây được thực thi bằng cách sử dụng await-generator.
- `FlowItem->throwIfInvalidNumber()` is now private.
  - Sử dụng `FlowItem->getInt()` và `FlowItem->getFloat()` thay thế.
- Các phương thức sau đã được di chuyển / đổi tên
  - `FlowItem->execute()` -> `FlowItem->onExecute()`
  - `FlowItemExecutor->executeGenerator()` -> `FlowItemExecutor->getExecutor()`
- Chữ ký của các phương thức sau đã được thay đổi
  - `FlowItem->__construct()` -> `FlowItem->__construct(string $id, string $category, array $permissions = [])`
  - `FlowItem->getPermission(): int` -> `FlowItem->getPermissions(): string[]`
  - `FlowItem->loadSaveData(array $content): FlowItem` -> `FlowItem->loadSaveData(array $content): void`
  - `FlowItemExecutor->execute(bool $first = true): bool` -> `FlowItemExecutor->execute(): bool`
- Các trait sau đã được thêm vào
  - `aieuo\mineflow\flowItem\base\ActionNameWithMineflowLanguage`
  - `aieuo\mineflow\flowItem\base\ConditionNameWithMineflowLanguage`
- Các phương thức sau đã được thêm vào
  - `FlowItem->setPermissions()`
- Các phương thức sau đã được xóa
  - `FlowItem->throwIfCannotExecute()`
  - `FlowItem->getEditFormElements()`
    - Sử dụng `FlowItem->buildEditForm()` thay thế.
  - `FlowItem->parseFromFormData()`
    - Sử dụng `EditFormResponseProcessor->preprocess()` thay thế.
  - `EntityFlowItem->throwIfInvalidEntity()`
    - Sử dụng `EntityFlowItem->getOnlineEntity()` thay thế.
  - `PlayerFlowItem->throwIfInvalidPlayer()`
    - Sử dụng `PlayerFlowItem->getOnlinePlayer()` thay thế.
  - `FlowItemExecutor->resume()`
  - `FlowItemExecutor->exit()`
- Các thuộc tính sau đã được xóa
  - `FlowItem->name`
    - Sử dụng `FlowItem->getName()`, `ActionNameWithMineflowLanguage`, hoặc `ConditionNameWithMineflowLanguage` thay thế.
  - `FlowItem->detail`
    - Sử dụng `FlowItem->getDetail()`, `ActionNameWithMineflowLanguage->getDetailReplaces()`, hoặc `ConditionNameWithMineflowLanguage->getDetailReplaces()` thay thế.
  - `FlowItem->detailDefaultReplace`
    - Sử dụng `ActionNameWithMineflowLanguage->getDetailDefaultReplaces()`, hoặc `ConditionNameWithMineflowLanguage->getDetailDefaultReplaces()` thay thế.

### Trigger

- Dữ liệu kích hoạt không còn được lưu trữ trong lớp `Trigger`
- Các lớp sau đã được di chuyển / đổi tên
  - `aieuo\mineflow\ui\trigger\TriggerForm` -> `aieuo\mineflow\trigger\TriggerForm`
  - `aieuo\mineflow\ui\trigger\BaseTriggerForm` -> `aieuo\mineflow\trigger\BaseTriggerForm`
- Các phương thức sau đã được di chuyển / đổi tên
  - `Trigger->__construct(string $type, string $key, string $subKey = "")` -> `Trigger->__construct(string $type)`
  - `Trigger->getVariables($data)` -> `Trigger->getVariables(mixed $data)`
  - `Triggers::getTrigger()` -> `Triggers::deserialize()`
  - `TriggerForm->sendAddedTriggerMenu` -> `TriggerForm->buildAddedTriggerMenu()`
  - `EventTrigger->isEnabled()` -> `EventManager->isTriggerEnabled()`
  - `EventTrigger->setEnabled()` -> `EventManager->setTriggerEnabled()`, `EventManager->setTriggerDisabled()`
- Các phương thức sau đã được thêm vào
  - `Trigger->hash()`
  - `Trigger->equals()`
  - `Trigger->serialize()`
  - `Trigger::deserialize()`
  - `TriggerHolder->getRecipesByType()`
  - `EventManager->isTriggerEnabledByEventName()`
  - `EventTriggerListener->unregisterEvent()`
  - `EventTriggerListener->getRegisteredListener()`
- Các phương thức sau đã được xóa
  - `Trigger::create()`
  - `Trigger->getKey()`
  - `Trigger->setKey()`
  - `Trigger->getSubKey()`
  - `Trigger->setSubKey()`
  - `TriggerHolder->getRecipesWithSubKey()`

# 3.1.0

- Đã thêm các hành động mới
  - Đặt tin nhắn cho sự kiện Khi người chơi trò chuyện
  - Đặt gamerule
- Đã sửa sự cố khi chỉnh sửa một số hành động

# 3.2.0

- Đã thêm các phương thức biến mới
  - Biến danh sách
    - random()
    - shuffle()
    - take(count)
    - take_last(count)
- Đã sửa [#235](https://github.com/aieuo/Mineflow/issues/235)
- Đã sửa biểu mẫu cài đặt ngôn ngữ không được hiển thị

# 3.3.0

- Đã thêm hỗ trợ cho pmmp 5.0
- Đã thêm các điều kiện mới
  - Hai vật phẩm giống nhau
  - Hai khối giống nhau

# 3.4.0

- Giờ đây có thể chỉ định URL cho hình ảnh nút biểu mẫu tùy chỉnh

## API

### Trigger

- TriggerHolder giờ đây có thể tạo nhiều thể hiện

### Recipe

- Đã thêm tham số tùy chọn để vô hiệu hóa việc cập nhật trình giữ kích hoạt khi thêm/xóa kích hoạt vào/từ công thức
- Giờ đây có thể chỉ định lớp Recipe cho trình quản lý công thức
- Đã thêm `RecipeManager::unload`

### FlowItem

- `FlowItemExecutor` giờ đây có thể được khởi động lại trong quá trình thực thi mục luồng
- `FlowItem` giờ sử dụng `FlowItemArgument` để quản lý đối số
  - Giao diện `*FlowItem` và trait `*FlowItemTrait` đã bị xóa
    - Sử dụng `FlowItem->setArguments()` và `FlowItemArgument` thay thế

### Command

- Đã đổi tên `CommandManager->getOriginCommand()` thành `CommandManager->getCommandLabel()`

### Form

- Hàm gọi lại của `CommandForm->sendAddCommand` giờ trả về lệnh đã thêm
- Văn bản tùy chọn thả xuống giờ được chuyển đến `Language::replace()`

### Mineflow

- `Mineflow::setEnabledRecipeErrorInConsole` đã được chuyển đến `Logger::setEnabledRecipeErrorInConsole`
